# ~/.tmuxinator/scorbot-web.yml

name: Om Async Tutorial
root: ~/src/sandbox/om-async

# Optional tmux socket
# socket_name: foo

# Runs before everything. Use it to start daemons etc.
pre: boot2docker up

# Runs in each window and pane before window/pane specific commands. Useful for setting up interpreter versions.
pre_window: direnv allow

# Pass command line options to tmux. Useful for specifying a different tmux.conf.
# tmux_options: -f ~/.tmux.mac.conf

# Change the command to call tmux.  This can be used by derivatives/wrappers like byobu.
# tmux_command: byobu

windows:
  - emacs: emacs
  - shell:
  - datomic: datomic/bin/transactor ~/src/sandbox/om-async/config/dev-transactor.properties
  - server: lein ring server
  - figwheel: lein figwheel
  - console: datomic/bin/console -p 9090 omasyncdb datomic:dev://omasyncdb:4334/omasyncdb
